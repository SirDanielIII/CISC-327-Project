Running the webapp:
1. Install all requirded python modules using pip and the requierments.txt file
2. Run the app.py in the app directory with python
3. Visit http://127.0.0.1:5000 in a browser

Note: The implementation currently uses in memory lists and will not persist data between runs.